global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'ts-agent-proxy'
    metrics_path: '/metrics/prometheus'
    scrape_interval: 5s
    static_configs:
      - targets: ['proxy:9090']
        labels:
          service: 'ts-agent-proxy'
